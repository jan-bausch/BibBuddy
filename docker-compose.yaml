version: "3"
services:

  backend:
    build: ./backend
    ports: ['5001:5000']
    volumes: ['./backend:/app']

  frontend:
    build: ./frontend
    ports: ['5000:1234']
    volumes: ['./frontend:/app']
